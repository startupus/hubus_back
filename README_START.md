# 🚀 Быстрый запуск проекта

## Одна команда для запуска

### Windows PowerShell:

```powershell
.\start.ps1 -Build
```

### Без сборки (если образы уже собраны):

```powershell
.\start-simple.ps1
```

## Что делает скрипт

1. ✅ Проверяет наличие Docker
2. ✅ Останавливает существующие контейнеры
3. ✅ Собирает образы всех сервисов (если нужно)
4. ✅ Запускает инфраструктурные сервисы (БД, Redis, RabbitMQ)
5. ✅ Ждет готовности инфраструктуры
6. ✅ Запускает все сервисы приложения
7. ✅ Показывает статус и доступные URL

## Параметры

- `-Build` - принудительная сборка образов
- `-NoCache` - сборка без кэша (для `start.ps1 -Build -NoCache`)

## После запуска

- 🌐 **Frontend**: http://localhost:80
- 🔌 **API Gateway**: http://localhost:3000
- 📊 **RabbitMQ**: http://localhost:15672 (guest/guest)

## Управление

```powershell
# Остановить все
docker compose down

# Просмотр логов
docker compose logs -f api-gateway

# Перезапустить сервис
docker compose restart api-gateway

# Статус
docker compose ps
```

## Решение проблем

**Ошибка сборки?**
```powershell
.\start.ps1 -Build -NoCache
```

**Порты заняты?**
```powershell
netstat -ano | findstr :3000
```

**Не хватает памяти?**
- Увеличьте лимит RAM в Docker Desktop (Settings → Resources)

