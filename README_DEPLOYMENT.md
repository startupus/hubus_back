# 🚀 Инструкция по развертыванию AI Aggregator

## 📋 Требования к системе

### Обязательные компоненты:
- **Docker Desktop** (версия 4.0+)
- **Docker Compose** (версия 2.0+)
- **Git** (для клонирования репозитория)
- **PowerShell** (для Windows) или **Bash** (для Linux/Mac)

### Системные требования:
- **RAM**: минимум 8GB (рекомендуется 16GB)
- **Диск**: минимум 10GB свободного места
- **CPU**: 4+ ядра
- **OS**: Windows 10/11, macOS, или Linux

---

## 🔧 Установка и настройка

### 1. Установка Docker Desktop

#### Windows:
1. Скачайте Docker Desktop с [официального сайта](https://www.docker.com/products/docker-desktop/)
2. Запустите установщик `Docker Desktop Installer.exe`
3. Следуйте инструкциям установщика
4. Перезагрузите компьютер
5. Запустите Docker Desktop и дождитесь полной загрузки

#### macOS:
1. Скачайте Docker Desktop для Mac с [официального сайта](https://www.docker.com/products/docker-desktop/)
2. Перетащите Docker.app в папку Applications
3. Запустите Docker Desktop из Applications
4. Следуйте инструкциям настройки

#### Linux (Ubuntu/Debian):
```bash
# Обновляем пакеты
sudo apt update

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавляем пользователя в группу docker
sudo usermod -aG docker $USER

# Устанавливаем Docker Compose
sudo apt install docker-compose-plugin

# Перезагружаемся
sudo reboot
```

### 2. Проверка установки

Откройте терминал/PowerShell и выполните:

```bash
# Проверяем Docker
docker --version
docker-compose --version

# Должны увидеть что-то вроде:
# Docker version 24.0.7, build afdd53b
# Docker Compose version v2.21.0
```

---

## 📥 Клонирование и запуск проекта

### 1. Клонирование репозитория

```bash
# Клонируем репозиторий
git clone https://github.com/teramisuslik/MVP.git
cd MVP
```

### 2. Проверка файлов проекта

Убедитесь, что в папке есть следующие файлы:
- `docker-compose.yml` ✅
- `test-complete-system.ps1` ✅
- Папка `services/` с микросервисами ✅

### 3. Запуск проекта

#### Windows (PowerShell):
```powershell
# Переходим в папку проекта
cd MVP

# Запускаем все сервисы
docker-compose up -d

# Ждем 2-3 минуты для полной инициализации
Start-Sleep -Seconds 180

# Проверяем статус
docker-compose ps
```

#### Linux/macOS (Bash):
```bash
# Переходим в папку проекта
cd MVP

# Запускаем все сервисы
docker-compose up -d

# Ждем 2-3 минуты для полной инициализации
sleep 180

# Проверяем статус
docker-compose ps
```

---

## ✅ Проверка работоспособности

### 1. Проверка статуса контейнеров

```bash
docker-compose ps
```

**Ожидаемый результат:**
- Все контейнеры должны быть в статусе `Up` или `Up (healthy)`
- Порты должны быть доступны (3000-3009)

### 2. Автоматическое тестирование

#### Windows:
```powershell
# Запускаем полный тест системы
.\test-complete-system.ps1
```

#### Linux/macOS:
```bash
# Создаем bash версию теста (если нужно)
chmod +x test-complete-system.ps1
# Или запускаем через PowerShell Core
pwsh test-complete-system.ps1
```

### 3. Ручная проверка основных endpoints

```bash
# Проверяем API Gateway
curl http://localhost:3000/health

# Проверяем Models (должны работать БЕЗ аутентификации)
curl http://localhost:3000/v1/models

# Проверяем Auth Service
curl http://localhost:3001/health
```

---

## 🌐 Доступные сервисы

После успешного запуска будут доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| **API Gateway** | http://localhost:3000 | Главный API |
| **Auth Service** | http://localhost:3001 | Аутентификация |
| **Provider Orchestrator** | http://localhost:3002 | Управление провайдерами |
| **Proxy Service** | http://localhost:3003 | Прокси к ИИ |
| **Billing Service** | http://localhost:3004 | Биллинг |
| **Analytics Service** | http://localhost:3005 | Аналитика |
| **Payment Service** | http://localhost:3006 | Платежи |
| **AI Certification** | http://localhost:3007 | Сертификация ИИ |
| **Redis Service** | http://localhost:3009 | Кэширование |
| **RabbitMQ Management** | http://localhost:15672 | Очереди сообщений |

---

## 🔧 Управление проектом

### Остановка сервисов:
```bash
docker-compose down
```

### Перезапуск сервисов:
```bash
docker-compose restart
```

### Просмотр логов:
```bash
# Все сервисы
docker-compose logs

# Конкретный сервис
docker-compose logs api-gateway
docker-compose logs auth-service
```

### Полная пересборка:
```bash
# Останавливаем
docker-compose down

# Удаляем старые образы
docker system prune -f

# Пересобираем с нуля
docker-compose build --no-cache

# Запускаем
docker-compose up -d
```

---

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверяем логи
docker-compose logs

# Проверяем доступность портов
netstat -an | grep :3000

# Перезапускаем Docker Desktop
# (Windows: перезапуск через системный трей)
```

### Проблема: Порты заняты

**Решение:**
```bash
# Находим процесс, использующий порт
netstat -ano | findstr :3000

# Останавливаем процесс (Windows)
taskkill /PID <PID> /F

# Или измените порты в docker-compose.yml
```

### Проблема: Недостаточно памяти

**Решение:**
1. Увеличьте память для Docker Desktop (Settings → Resources → Memory)
2. Закройте другие приложения
3. Перезапустите Docker Desktop

### Проблема: Models endpoints требуют аутентификацию

**Решение:**
Это исправлено в последней версии. Если проблема остается:
```bash
# Пересоберите API Gateway
docker-compose build --no-cache api-gateway
docker-compose up -d api-gateway
```

---

## 📊 Мониторинг системы

### Проверка здоровья всех сервисов:
```bash
# Автоматический тест
.\test-complete-system.ps1

# Ручная проверка
curl http://localhost:3000/health
curl http://localhost:3001/health
curl http://localhost:3002/health
# ... и так далее
```

### Просмотр метрик:
- **RabbitMQ**: http://localhost:15672 (admin/admin)
- **Docker Desktop**: встроенный мониторинг
- **Логи**: `docker-compose logs -f`

---

## 🎯 Ожидаемые результаты

После успешного запуска вы должны увидеть:

### ✅ Статус контейнеров:
```
NAME                                 STATUS
project-api-gateway-1                Up
project-auth-service-1               Up
project-provider-orchestrator-1      Up
project-proxy-service-1              Up
project-billing-service-1            Up
project-analytics-service-1          Up
project-payment-service-1            Up
project-ai-certification-service-1   Up
project-redis-service-1              Up
```

### ✅ Результаты тестирования:
```
Total Tests:  21
Passed:       21 ✅
Failed:       0 ❌
Success Rate: 100% 🎯
```

### ✅ Доступные API:
- **Models**: http://localhost:3000/v1/models (БЕЗ аутентификации)
- **Auth**: http://localhost:3000/v1/auth/login
- **Health**: http://localhost:3000/health

---

## 🆘 Поддержка

Если возникли проблемы:

1. **Проверьте логи**: `docker-compose logs`
2. **Перезапустите**: `docker-compose restart`
3. **Полная пересборка**: см. раздел "Полная пересборка"
4. **Проверьте системные требования**: RAM, диск, порты

### Полезные команды для диагностики:

```bash
# Статус всех контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Свободное место на диске
df -h

# Использование памяти
free -h

# Проверка портов
netstat -tulpn | grep :300
```

---

## 🎉 Готово!

После выполнения всех шагов у вас будет полностью рабочая микросервисная система AI Aggregator с:

- ✅ 9 работающими сервисами
- ✅ 100% успешностью тестов
- ✅ Публичными Models endpoints
- ✅ Полной аутентификацией
- ✅ Биллингом и аналитикой
- ✅ Мониторингом и логированием

**Система готова к использованию!** 🚀

---

*Инструкция создана: 2025-10-12*  
*Версия проекта: 1.0.0*  
*Статус: Production Ready* ✅
