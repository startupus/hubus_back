# 🧠 Система рекомендаций нейросетей

## 📋 Обзор

Реализована система рекомендаций нейросетей на основе статистики популярности с поддержкой российских нейросетей по умолчанию.

## 🎯 Основные возможности

### ✅ Статистика популярности
- **Подсчет запросов** - количество запросов к каждой нейросети
- **Анализ токенов** - общее количество обработанных токенов
- **Расчет стоимости** - общая стоимость использования
- **Уникальные пользователи** - количество уникальных пользователей
- **Время ответа** - среднее время ответа нейросети
- **Процент успеха** - процент успешных запросов

### ✅ Система рекомендаций
- **Персональные рекомендации** - на основе истории пользователя
- **Популярные нейросети** - на основе общей статистики
- **Российские по умолчанию** - три российские нейросети при отсутствии статистики
- **Гибкие фильтры** - по лимиту, включению российских и т.д.

### ✅ Российские нейросети по умолчанию
1. **Yandex GPT** - российская языковая модель (скор: 100)
2. **GigaChat** - ИИ-модель от Сбера (скор: 95)
3. **Kandinsky** - генерация изображений от Сбера (скор: 90)

## 🏗️ Архитектура

### База данных (Analytics Service)

```prisma
model NeuralNetworkStats {
  id              String   @id @default(uuid())
  provider        String   // openai, anthropic, yandex, sber, etc.
  model           String   // gpt-3.5-turbo, claude-3-sonnet, yandex-gpt, etc.
  totalRequests   Int      @default(0)
  totalTokens     Int      @default(0)
  totalCost       Decimal  @default(0)
  uniqueUsers     Int      @default(0)
  avgResponseTime Int      @default(0) // milliseconds
  successRate     Decimal  @default(0) // percentage
  lastUsed        DateTime @default(now())
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model NeuralNetworkRecommendation {
  id              String   @id @default(uuid())
  userId          String?  // null for global recommendations
  provider        String
  model           String
  reason          String   // 'popular', 'russian', 'fast', 'cheap', etc.
  score           Decimal  @db.Decimal(5, 2) // 0-100
  isDefault       Boolean  @default(false) // for Russian defaults
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

### Сервисы

#### NeuralNetworkRecommendationService
- **updateNeuralNetworkStats()** - обновление статистики
- **getRecommendations()** - получение рекомендаций
- **getTopPopular()** - топ популярных нейросетей
- **getProviderStats()** - статистика по провайдеру
- **createPersonalRecommendation()** - персональные рекомендации
- **initializeRussianDefaults()** - инициализация российских по умолчанию

#### API Endpoints

**Analytics Service (порт 3005):**
- `GET /neural-networks/recommendations` - рекомендации
- `GET /neural-networks/popular` - популярные
- `GET /neural-networks/stats/:provider` - статистика провайдера
- `GET /neural-networks/russian-defaults` - российские по умолчанию

**API Gateway (порт 3000):**
- `GET /chat/recommendations` - рекомендации через API Gateway
- `GET /chat/popular` - популярные через API Gateway

## 🔧 Алгоритм рекомендаций

### Расчет скора рекомендации
```typescript
const popularityScore = Math.min(totalRequests / 100, 50); // До 50 баллов
const speedScore = Math.max(0, 30 - avgResponseTime / 1000); // До 30 баллов
const reliabilityScore = successRate * 0.2; // До 20 баллов

const totalScore = popularityScore + speedScore + reliabilityScore;
```

### Приоритеты рекомендаций
1. **Персональные рекомендации** (если есть)
2. **Популярные на основе статистики** (по скору)
3. **Российские по умолчанию** (если включены и не хватает)

## 📊 API Примеры

### Получение рекомендаций
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  "http://localhost:3000/chat/recommendations?limit=5&includeRussian=true"
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "recommendations": [
      {
        "provider": "yandex",
        "model": "yandex-gpt",
        "reason": "russian",
        "score": 100,
        "isDefault": true,
        "description": "Yandex GPT - российская языковая модель"
      },
      {
        "provider": "openai",
        "model": "gpt-3.5-turbo",
        "reason": "popular",
        "score": 85,
        "isDefault": false,
        "stats": {
          "totalRequests": 300,
          "avgResponseTime": 800,
          "successRate": 98.5
        }
      }
    ],
    "total": 2,
    "hasRussianDefaults": true
  }
}
```

### Получение популярных
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  "http://localhost:3000/chat/popular?limit=3"
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "provider": "openai",
      "model": "gpt-3.5-turbo",
      "totalRequests": 300,
      "totalTokens": 100000,
      "totalCost": 20.0,
      "uniqueUsers": 45,
      "avgResponseTime": 800,
      "successRate": 98.5,
      "lastUsed": "2024-01-15T10:30:00Z"
    }
  ]
}
```

## 🚀 Установка и запуск

### 1. Обновление базы данных
```bash
# В analytics-service
cd services/analytics-service
npx prisma migrate dev --name add-neural-network-stats
npx prisma generate
```

### 2. Инициализация российских нейросетей
```powershell
.\init-russian-neural-networks.ps1
```

### 3. Тестирование
```powershell
.\test-neural-network-recommendations.ps1
```

## 📈 Мониторинг и метрики

### Ключевые метрики
- **Количество рекомендаций** - сколько рекомендаций выдано
- **Конверсия рекомендаций** - сколько рекомендаций использовано
- **Популярность российских** - использование российских нейросетей
- **Время ответа API** - производительность системы рекомендаций

### Логирование
```typescript
LoggerUtil.info('analytics-service', 'Neural network stats updated', {
  provider,
  model,
  requests,
  tokens,
  cost,
  userId
});
```

## 🔄 Интеграция с существующей системой

### Обновление статистики
Статистика автоматически обновляется при каждом запросе к нейросети через:
- `ChatController.createCompletion()` - при успешном запросе
- `AnalyticsService.trackEvent()` - при обработке событий

### Связь с биллингом
- Статистика стоимости интегрирована с `billing-service`
- Учет уникальных пользователей по `companyId`
- Отслеживание использования по провайдерам

## 🎯 Будущие улучшения

### Планируемые функции
- **Машинное обучение** - рекомендации на основе ML
- **A/B тестирование** - тестирование разных алгоритмов
- **Персонализация** - более точные персональные рекомендации
- **Кэширование** - кэш для быстрого доступа к рекомендациям
- **Уведомления** - уведомления о новых популярных нейросетях

### Оптимизации
- **Индексы БД** - оптимизация запросов
- **Партиционирование** - разделение больших таблиц
- **Агрегация** - предварительный расчет статистики
- **CDN** - кэширование статических рекомендаций

## 🧪 Тестирование

### Автоматические тесты
- Unit тесты для `NeuralNetworkRecommendationService`
- Integration тесты для API эндпоинтов
- E2E тесты для полного цикла рекомендаций

### Ручное тестирование
- Скрипт `test-neural-network-recommendations.ps1`
- Проверка всех API эндпоинтов
- Тестирование с разными параметрами

## 📝 Заключение

Система рекомендаций нейросетей успешно реализована и интегрирована в существующую архитектуру. Она обеспечивает:

✅ **Умные рекомендации** на основе реальной статистики использования  
✅ **Российские нейросети по умолчанию** для поддержки отечественных решений  
✅ **Гибкую настройку** параметров и фильтров  
✅ **Высокую производительность** с оптимизированными запросами  
✅ **Полную интеграцию** с существующими сервисами  

Система готова к использованию в продакшене и может быть легко расширена дополнительными функциями.
