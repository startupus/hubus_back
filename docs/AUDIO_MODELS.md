# Модели с поддержкой аудио/видео в OpenRouter

## ✅ Найденные модели с поддержкой аудио

### 1. **openai/gpt-4o-audio-preview** ⭐ РЕКОМЕНДУЕТСЯ
- **Название**: OpenAI: GPT-4o Audio
- **Описание**: Модель добавляет поддержку аудио входов в промптах. Может обнаруживать нюансы в аудио записях.
- **Возможности**: chat, completion, vision, function_calling
- **Цена**: $40 за миллион входных аудио токенов
- **Важно**: Аудио выходы пока не поддерживаются
- **Поддержка видео**: ❌ **НЕТ** - только аудио, не видео
- **Использование**: Через `chat/completions` с аудио в сообщениях

### 2. **mistralai/voxtral-small-24b-2507** ⭐ РЕКОМЕНДУЕТСЯ
- **Название**: Mistral: Voxtral Small 24B 2507
- **Описание**: Улучшенная версия Mistral Small 3 с передовыми возможностями обработки аудио. Отлично справляется с транскрипцией речи, переводом и пониманием аудио.
- **Возможности**: chat, completion
- **Цена**: $100 за миллион секунд входного аудио
- **Поддержка видео**: ❌ **НЕТ** - только аудио, не видео
- **Использование**: Через `chat/completions` с аудио в сообщениях

### 3. **openai/gpt-4o** ✅ ПОДДЕРЖИВАЕТ ВИДЕО
- **Название**: OpenAI: GPT-4o
- **Описание**: Мультимодальная модель, поддерживает текст, изображения, **аудио и видео** на входе. Может генерировать текст, аудио и изображения (но не видео).
- **Возможности**: chat, completion, vision, function_calling
- **Поддержка видео**: ✅ **ДА** - может принимать видео на вход
- **Поддержка аудио**: ✅ **ДА** - может принимать аудио на вход
- **Использование**: Через `chat/completions` с видео/аудио в сообщениях

### 4. **openai/gpt-4-turbo**
- **Название**: OpenAI: GPT-4 Turbo
- **Описание**: Последняя модель GPT-4 Turbo с возможностями vision. Неясно, поддерживает ли аудио.
- **Возможности**: chat, completion, vision, function_calling
- **Использование**: Возможно через `chat/completions` (требует проверки)

## ⚠️ Важные ограничения

1. **OpenRouter НЕ поддерживает endpoint `/audio/transcriptions`**
   - Whisper API доступен только через прямой OpenAI API
   - OpenRouter не предоставляет прямой доступ к Whisper

2. **Аудио обрабатывается через `chat/completions`**
   - Аудио передается в сообщениях как base64 или URL
   - Формат зависит от конкретной модели

3. **Поддержка видео:**
   - ✅ **GPT-4o** (обычный) - поддерживает видео на входе
   - ❌ **GPT-4o-audio-preview** - только аудио, не видео
   - ❌ **Voxtral** - только аудио, не видео

4. **Не все модели GPT-4o поддерживают аудио**
   - Только `gpt-4o-audio-preview` специально для аудио
   - Обычный `gpt-4o` поддерживает и аудио, и видео

## 📝 Рекомендации для Habus

### Вариант 1: Использовать GPT-4o Audio Preview (через OpenRouter)
```json
{
  "model": "openai/gpt-4o-audio-preview",
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "Транскрибируй это аудио"
        },
        {
          "type": "audio",
          "audio": {
            "url": "data:audio/mpeg;base64,..."
          }
        }
      ]
    }
  ]
}
```

### Вариант 2: Использовать Voxtral (через OpenRouter)
```json
{
  "model": "mistralai/voxtral-small-24b-2507",
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "Транскрибируй это аудио на русском"
        },
        {
          "type": "audio",
          "audio": {
            "url": "data:audio/mpeg;base64,..."
          }
        }
      ]
    }
  ]
}
```

### Вариант 3: Использовать прямой OpenAI API для Whisper
- Endpoint: `POST /v1/audio/transcriptions`
- Требует `OPENAI_API_KEY`
- Формат: multipart/form-data
- Это стандартный Whisper API

## 🔍 Как проверить поддержку аудио

1. Проверить список моделей через API:
   ```bash
   GET /api/v1/models
   ```

2. Искать модели с:
   - `id` содержащим `audio`, `whisper`, `voxtral`
   - `capabilities` содержащим `audio`
   - `description` упоминающим audio/transcription

3. Проверить документацию OpenRouter для конкретной модели

## 💡 Итоговая рекомендация

### Для обработки аудио:
1. **Прямой OpenAI API для Whisper** - если нужен стандартный endpoint `/v1/audio/transcriptions`
2. **GPT-4o Audio Preview через OpenRouter** - если можно использовать `chat/completions` с аудио
3. **Voxtral через OpenRouter** - альтернатива для транскрипции через `chat/completions`

### Для обработки видео:
1. **GPT-4o через OpenRouter** ✅ - поддерживает видео на входе через `chat/completions`
2. **GPT-4o-audio-preview** ❌ - НЕ поддерживает видео
3. **Voxtral** ❌ - НЕ поддерживает видео

**Важно**: 
- OpenRouter не поддерживает стандартный Whisper endpoint, поэтому если Habus требует именно `/v1/audio/transcriptions`, нужно использовать прямой OpenAI API.
- Для обработки видео используйте обычный `gpt-4o`, а не `gpt-4o-audio-preview`
- `gpt-4o-audio-preview` и `voxtral` работают только с аудио, не с видео

