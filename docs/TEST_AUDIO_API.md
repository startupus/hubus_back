# Тестирование отправки аудио через API

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install axios
npm install -D ts-node @types/node
```

### Запуск теста

```bash
# С тестовым файлом (минимальный WAV)
npx ts-node test-audio-api.ts

# С реальным аудио файлом
npx ts-node test-audio-api.ts path/to/your/audio.mp3
```

## 📝 Примеры использования

### Тест с MP3 файлом

```bash
npx ts-node test-audio-api.ts audio.mp3
```

### Тест с WAV файлом

```bash
npx ts-node test-audio-api.ts audio.wav
```

### Тест с M4A файлом

```bash
npx ts-node test-audio-api.ts audio.m4a
```

## 🔧 Настройка

Перед запуском убедитесь, что:

1. **Контейнеры запущены**:
   ```bash
   docker-compose ps
   ```

2. **API ключ настроен** в скрипте:
   ```typescript
   const API_KEY = 'ak_your_api_key_here';
   ```

3. **OpenRouter API ключ** настроен в `.env`:
   ```bash
   OPENROUTER_API_KEY=sk-or-v1-your-key
   ```

## 📊 Формат ответа

Скрипт выводит:
- ✅ Статус выполнения
- 📋 Текст транскрипции от нейросети
- 📊 Статистику использования токенов
- ⏱️ Время обработки

## ⚠️ Важные замечания

1. **Тестовый файл**: По умолчанию создается минимальный WAV файл без реальных аудио данных. Для реальной транскрипции используйте реальный аудио файл.

2. **Поддерживаемые форматы**:
   - WAV
   - MP3
   - M4A
   - AAC
   - OGG
   - FLAC
   - AIFF

3. **Размер файлов**: 
   - **Максимальный размер: 50MB** (настроено в системе)
   - Рекомендуется использовать файлы до 50MB для аудио

4. **Таймаут**: Установлен на 5 минут (300 секунд) для обработки больших файлов.

## 🐛 Отладка

Если возникают ошибки:

1. **Проверьте логи сервисов**:
   ```bash
   docker-compose logs api-gateway proxy-service
   ```

2. **Проверьте статус контейнеров**:
   ```bash
   docker-compose ps
   ```

3. **Проверьте API ключ**:
   - Убедитесь, что API ключ правильный
   - Проверьте, что ключ активен в базе данных

4. **Проверьте OpenRouter ключ**:
   - Убедитесь, что `OPENROUTER_API_KEY` настроен в `.env`
   - Проверьте, что ключ валидный на https://openrouter.ai

## 📚 Связанные документы

- [Инструкция по отправке аудио/видео](./EXTERNAL_API_AUDIO_VIDEO.md)
- [Примеры использования API](../API_EXAMPLES.md)

