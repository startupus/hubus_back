# Analytics Service

Комплексный сервис аналитики для платформы AI Aggregator, обеспечивающий сбор, обработку, анализ и экспорт данных о работе системы.

## 🚀 Возможности

### 📊 Сбор и обработка данных
- **События**: Отслеживание пользовательских действий и системных событий
- **Метрики**: Сбор показателей производительности и использования
- **Пакетная обработка**: Эффективная обработка больших объемов данных
- **Валидация**: Проверка корректности входящих данных

### 📈 Аналитика и отчетность
- **Дашборды**: Интерактивные панели с ключевыми показателями
- **Отчеты**: Детальные отчеты по использованию и производительности
- **Экспорт данных**: CSV, JSON, Excel, PDF форматы
- **Рекомендации**: Персонализированные рекомендации по оптимизации

### 🔗 Интеграции
- **Prometheus**: Экспорт метрик для мониторинга
- **Grafana**: Создание дашбордов и визуализаций
- **Webhooks**: Отправка данных во внешние системы
- **API Gateway**: Интеграция с основным шлюзом

### 🛡️ Безопасность и надежность
- **Аутентификация**: JWT токены для доступа к API
- **Rate Limiting**: Ограничение частоты запросов
- **Валидация данных**: Проверка входящих данных
- **Обработка ошибок**: Graceful error handling

## 🏗️ Архитектура

```
analytics-service/
├── src/
│   ├── common/           # Общие компоненты
│   │   └── prisma/       # Prisma ORM сервис
│   ├── config/           # Конфигурация
│   ├── controllers/      # API контроллеры
│   ├── integrations/     # Внешние интеграции
│   ├── services/         # Бизнес-логика
│   ├── types/            # TypeScript типы
│   └── dto/              # Data Transfer Objects
├── prisma/               # База данных
│   └── schema.prisma     # Схема БД
└── package.json
```

## 📋 Требования

- Node.js 18+
- PostgreSQL 14+
- Redis 6+
- Docker & Docker Compose

## 🚀 Установка и запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка базы данных
```bash
# Генерация Prisma клиента
npm run db:generate

# Применение миграций
npm run db:migrate

# Заполнение тестовыми данными (опционально)
npm run db:seed
```

### 3. Настройка переменных окружения
Скопируйте `.env.example` в `.env` и настройте переменные:

```env
# База данных
ANALYTICS_DATABASE_URL=postgresql://postgres:password@localhost:5432/analytics_db

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=your-secret-key

# Интеграции
PROMETHEUS_ENABLED=true
PROMETHEUS_ENDPOINT=http://localhost:9090
GRAFANA_ENABLED=true
GRAFANA_API_URL=http://localhost:3000
GRAFANA_API_KEY=your-api-key
```

### 4. Запуск сервиса
```bash
# Разработка
npm run start:dev

# Продакшн
npm run build
npm run start:prod
```

## 📚 API Документация

### Основные эндпоинты

#### События
- `POST /analytics/events/track` - Отслеживание события
- `POST /analytics/events/batch` - Пакетная отправка событий
- `GET /analytics/events` - Получение событий с фильтрацией

#### Метрики
- `POST /analytics/metrics/record` - Запись метрики
- `POST /analytics/metrics/batch` - Пакетная запись метрик
- `GET /analytics/metrics` - Получение метрик

#### Пользовательская аналитика
- `GET /analytics/users/:userId/analytics` - Аналитика пользователя

#### Дашборды
- `GET /analytics/dashboard` - Основной дашборд
- `GET /analytics/health` - Состояние системы

#### Отчеты
- `POST /reports/exports` - Создание экспорта
- `GET /reports/exports/:id/status` - Статус экспорта
- `GET /reports/exports/:id/download` - Скачивание файла
- `GET /reports/usage/:userId` - Отчет по использованию
- `GET /reports/system-health` - Отчет по состоянию системы

### Примеры запросов

#### Отслеживание события
```bash
curl -X POST http://localhost:3005/analytics/events/track \
  -H "Content-Type: application/json" \
  -d '{
    "eventType": "user_action",
    "eventName": "login",
    "service": "auth-service",
    "userId": "user123",
    "properties": {
      "method": "email",
      "success": true
    }
  }'
```

#### Запись метрики
```bash
curl -X POST http://localhost:3005/analytics/metrics/record \
  -H "Content-Type: application/json" \
  -d '{
    "service": "api-gateway",
    "metricType": "performance",
    "metricName": "response_time",
    "value": 150,
    "unit": "ms",
    "labels": {
      "endpoint": "/api/chat",
      "method": "POST"
    }
  }'
```

#### Получение дашборда
```bash
curl -X GET "http://localhost:3005/analytics/dashboard?userId=user123"
```

## 🗄️ База данных

### Основные таблицы

- **analytics_events** - События системы
- **metrics_snapshots** - Снимки метрик
- **user_analytics** - Пользовательская аналитика
- **ai_analytics** - Аналитика ИИ моделей
- **system_health** - Состояние системы
- **alerts** - Уведомления и алерты

### Индексы для производительности

- Составные индексы по времени и типу событий
- Индексы по пользователям и сервисам
- Индексы для быстрого поиска метрик

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `ANALYTICS_SERVICE_PORT` | Порт сервиса | 3005 |
| `ANALYTICS_DATABASE_URL` | URL базы данных | - |
| `REDIS_URL` | URL Redis | redis://localhost:6379 |
| `JWT_SECRET` | Секретный ключ JWT | - |
| `PROMETHEUS_ENABLED` | Включить Prometheus | false |
| `GRAFANA_ENABLED` | Включить Grafana | false |

### Настройка интеграций

#### Prometheus
```env
PROMETHEUS_ENABLED=true
PROMETHEUS_ENDPOINT=http://localhost:9090
```

#### Grafana
```env
GRAFANA_ENABLED=true
GRAFANA_API_URL=http://localhost:3000
GRAFANA_API_KEY=your-api-key
GRAFANA_DATASOURCE_NAME=analytics-db
```

#### Webhooks
```env
WEBHOOKS='[
  {
    "id": "slack",
    "url": "https://hooks.slack.com/...",
    "events": ["alert", "critical"],
    "enabled": true
  }
]'
```

## 📊 Мониторинг

### Health Check
```bash
curl http://localhost:3005/analytics/ping
```

### Статистика сбора данных
```bash
curl http://localhost:3005/analytics/stats/collection
```

### Состояние системы
```bash
curl http://localhost:3005/analytics/health
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
npm test

# Тесты с покрытием
npm run test:cov

# E2E тесты
npm run test:e2e

# Тесты в режиме отладки
npm run test:debug
```

### Тестовые данные
```bash
# Заполнение тестовыми данными
npm run db:seed
```

## 🚀 Развертывание

### Docker
```bash
# Сборка образа
docker build -t analytics-service .

# Запуск контейнера
docker run -p 3005:3005 \
  -e ANALYTICS_DATABASE_URL=postgresql://... \
  -e JWT_SECRET=... \
  analytics-service
```

### Docker Compose
```yaml
version: '3.8'
services:
  analytics-service:
    build: ./services/analytics-service
    ports:
      - "3005:3005"
    environment:
      - ANALYTICS_DATABASE_URL=postgresql://postgres:password@analytics-db:5432/analytics_db
      - JWT_SECRET=your-secret
    depends_on:
      - analytics-db
      - redis

  analytics-db:
    image: postgres:14
    environment:
      - POSTGRES_DB=analytics_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    volumes:
      - analytics_data:/var/lib/postgresql/data

volumes:
  analytics_data:
```

## 🔍 Отладка

### Логи
```bash
# Просмотр логов
docker logs analytics-service

# Логи в реальном времени
docker logs -f analytics-service
```

### База данных
```bash
# Подключение к БД
psql postgresql://postgres:password@localhost:5432/analytics_db

# Prisma Studio
npm run db:studio
```

### Производительность
```bash
# Статистика БД
curl http://localhost:3005/analytics/stats/collection

# Медленные запросы
# (требует настройки pg_stat_statements)
```

## 📈 Масштабирование

### Горизонтальное масштабирование
- Использование Redis для кэширования
- Пакетная обработка событий
- Асинхронная обработка через RabbitMQ

### Оптимизация производительности
- Индексы базы данных
- Кэширование часто запрашиваемых данных
- Партиционирование больших таблиц

## 🤝 Разработка

### Структура проекта
- **Сервисы**: Бизнес-логика в `src/services/`
- **Контроллеры**: API эндпоинты в `src/controllers/`
- **Типы**: TypeScript типы в `src/types/`
- **DTO**: Валидация данных в `src/dto/`

### Добавление новых метрик
1. Добавьте тип в `src/types/analytics.types.ts`
2. Создайте DTO в `src/dto/analytics.dto.ts`
3. Добавьте обработку в сервис
4. Создайте эндпоинт в контроллере

### Добавление новых интеграций
1. Создайте сервис в `src/integrations/`
2. Добавьте конфигурацию
3. Зарегистрируйте в модуле

## 📝 Changelog

### v1.0.0
- Первоначальный релиз
- Сбор событий и метрик
- Базовые отчеты и дашборды
- Интеграция с Prometheus и Grafana
- Экспорт данных в различных форматах

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🆘 Поддержка

Для получения помощи:
- Создайте issue в репозитории
- Обратитесь к команде разработки
- Проверьте документацию API

---

**Analytics Service** - мощный инструмент для анализа и мониторинга платформы AI Aggregator.
