# API Gateway Service

## 🎯 Назначение

API Gateway - это единая точка входа для всех клиентских запросов. Обеспечивает маршрутизацию, аутентификацию, rate limiting и агрегацию ответов от микросервисов.

## 🏗️ Архитектура

```
Client Request → API Gateway → Microservices
     ↓              ↓              ↓
  Auth Check    Route Request   Process & Return
  Rate Limit    Load Balance    Aggregate Response
```

## 🚀 Запуск

```bash
# Запуск сервиса
docker-compose up -d api-gateway

# Проверка статуса
curl http://localhost:3000/health
```

## 📡 API Endpoints

### Аутентификация
- `POST /v1/auth/register` - Регистрация пользователя
- `POST /v1/auth/login` - Вход в систему
- `POST /v1/auth/api-keys` - Управление API ключами

### AI Запросы
- `POST /v1/chat/completions` - Chat completions
- `GET /v1/models` - Список доступных моделей

### Биллинг
- `GET /v1/billing/balance` - Баланс пользователя
- `GET /v1/billing/history` - История транзакций

### Аналитика
- `GET /v1/analytics/dashboard` - Аналитическая панель
- `POST /v1/analytics/events` - Отправка событий

## 🔧 Конфигурация

### Environment Variables
```env
PORT=3000
AUTH_SERVICE_URL=http://auth-service:3001
BILLING_SERVICE_URL=http://billing-service:3004
ORCHESTRATOR_SERVICE_URL=http://provider-orchestrator:3002
PROXY_SERVICE_URL=http://proxy-service:3003
ANALYTICS_SERVICE_URL=http://analytics-service:3005
```

### Зависимости
- Auth Service (gRPC)
- Billing Service (gRPC)
- Provider Orchestrator (HTTP)
- Proxy Service (HTTP)
- Analytics Service (HTTP)

## 🛡️ Безопасность

### Аутентификация
- JWT токены для пользователей
- API ключи для внешних интеграций
- Валидация всех входящих запросов

### Rate Limiting
- Ограничение запросов по пользователю
- Защита от DDoS атак
- Приоритизация запросов

## 📊 Мониторинг

### Health Check
```bash
curl http://localhost:3000/health
```

### Метрики
- Количество запросов
- Время ответа
- Ошибки
- Использование ресурсов

## 🔄 Интеграция с сервисами

### Синхронные вызовы
- Auth Service → валидация токенов
- Billing Service → проверка баланса
- Provider Orchestrator → маршрутизация

### Асинхронные вызовы
- Analytics Service → отправка метрик
- Billing Service → списание средств

## 🚨 Обработка ошибок

### Типы ошибок
- `400 Bad Request` - неверные данные
- `401 Unauthorized` - проблемы с аутентификацией
- `403 Forbidden` - недостаточно прав
- `429 Too Many Requests` - превышен лимит
- `500 Internal Server Error` - внутренние ошибки

### Fallback стратегии
- Резервные провайдеры
- Кэширование ответов
- Graceful degradation

## 📈 Производительность

### Оптимизации
- Кэширование в Redis
- Connection pooling
- Асинхронная обработка
- Load balancing

### Масштабирование
- Горизонтальное масштабирование
- Load balancer
- Auto-scaling

## 🔧 Разработка

### Структура проекта
```
src/
├── auth/           # Аутентификация
├── billing/        # Биллинг
├── orchestrator/   # Маршрутизация
├── proxy/          # Проксирование
├── analytics/      # Аналитика
└── common/         # Общие утилиты
```

### Тестирование
```bash
# Unit тесты
npm run test

# E2E тесты
npm run test:e2e

# Тестирование API
npm run test:api
```

## 📝 Логирование

### Уровни логирования
- `ERROR` - критические ошибки
- `WARN` - предупреждения
- `INFO` - информационные сообщения
- `DEBUG` - отладочная информация

### Формат логов
```json
{
  "timestamp": "2025-10-05T22:30:00.000Z",
  "level": "INFO",
  "service": "api-gateway",
  "message": "Request processed",
  "requestId": "req-123",
  "userId": "user-456",
  "duration": 150
}
```
