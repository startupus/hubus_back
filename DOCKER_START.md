# 🐳 Запуск проекта в Docker

## Быстрый старт

### 1. Предварительные требования

- **Docker Desktop** установлен и запущен
- **Docker Compose** (обычно входит в Docker Desktop)
- Минимум **8GB RAM** свободной памяти
- Минимум **10GB** свободного места на диске

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (если его еще нет):

```bash
# Скопируйте пример
cp env.example .env
```

Отредактируйте `.env` и укажите ваши API ключи:
- `OPENAI_API_KEY` - ключ OpenAI (если используете)
- `OPENROUTER_API_KEY` - ключ OpenRouter (если используете)
- `GITHUB_API_KEY` - ключ GitHub (если используете)
- `YANDEX_API_KEY` - ключ YandexGPT (если используете)
- `YANDEX_FOLDER_ID` - ID папки Yandex Cloud

> **Примечание:** Для локальной разработки можно использовать mock значения, но для реальных запросов к ИИ-провайдерам нужны настоящие ключи.

### 3. Запуск проекта

#### Вариант 1: Использование PowerShell скрипта (Windows)

```powershell
.\docker-start.ps1
```

#### Вариант 2: Ручной запуск

```bash
# Остановить существующие контейнеры
docker-compose down

# Собрать образы (первый запуск или после изменений)
docker-compose build

# Запустить все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### 4. Проверка статуса

```bash
# Статус всех контейнеров
docker-compose ps

# Проверка здоровья сервисов
docker-compose ps --format "table {{.Name}}\t{{.Status}}"
```

### 5. Доступные сервисы

После запуска доступны следующие endpoints:

| Сервис | URL | Описание |
|--------|-----|----------|
| **API Gateway** | http://localhost:3000 | Главный API endpoint |
| **Frontend** | http://localhost:80 | Веб-интерфейс |
| **Auth Service** | http://localhost:3001 | Сервис аутентификации |
| **Provider Orchestrator** | http://localhost:3002 | Управление провайдерами |
| **Proxy Service** | http://localhost:3003 | Прокси к ИИ-провайдерам |
| **Billing Service** | http://localhost:3004 | Биллинг и балансы |
| **Analytics Service** | http://localhost:3005 | Аналитика |
| **Payment Service** | http://localhost:3006 | Платежи |
| **RabbitMQ Management** | http://localhost:15672 | Управление очередями (guest/guest) |

### 6. Полезные команды

```bash
# Просмотр логов конкретного сервиса
docker-compose logs -f api-gateway
docker-compose logs -f auth-service
docker-compose logs -f billing-service

# Перезапуск сервиса
docker-compose restart api-gateway

# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes (удалит все данные БД!)
docker-compose down -v

# Пересборка конкретного сервиса
docker-compose build api-gateway
docker-compose up -d api-gateway

# Выполнение команд внутри контейнера
docker-compose exec api-gateway sh
docker-compose exec auth-service sh

# Просмотр использования ресурсов
docker stats
```

### 7. Базы данных

Все базы данных доступны локально на разных портах:

| База данных | Порт | Подключение |
|------------|------|-------------|
| auth-db | 5432 | `postgresql://postgres:password@localhost:5432/auth_db` |
| billing-db | 5433 | `postgresql://postgres:password@localhost:5433/billing_db` |
| orchestrator-db | 5434 | `postgresql://postgres:password@localhost:5434/orchestrator_db` |
| analytics-db | 5435 | `postgresql://postgres:password@localhost:5435/analytics_db` |
| certification-db | 5436 | `postgresql://postgres:password@localhost:5436/certification_db` |
| api-gateway-db | 5437 | `postgresql://postgres:password@localhost:5437/api_gateway_db` |
| anonymization-db | 5438 | `postgresql://postgres:password@localhost:5438/anonymization_db` |
| payment-db | 5439 | `postgresql://postgres:password@localhost:5439/payment_db` |

### 8. Миграции базы данных

Миграции выполняются автоматически при первом запуске сервисов. Если нужно выполнить миграции вручную:

```bash
# Для конкретного сервиса
docker-compose exec auth-service npx prisma migrate deploy
docker-compose exec billing-service npx prisma migrate deploy
```

### 9. Решение проблем

#### Сервисы не запускаются

1. Проверьте логи:
   ```bash
   docker-compose logs [service-name]
   ```

2. Проверьте, что все порты свободны:
   ```bash
   netstat -ano | findstr :3000
   ```

3. Убедитесь, что Docker Desktop запущен и имеет достаточно ресурсов (минимум 4GB RAM)

#### Ошибки подключения к БД

1. Проверьте, что базы данных запущены:
   ```bash
   docker-compose ps | grep db
   ```

2. Проверьте healthcheck:
   ```bash
   docker-compose ps
   ```

#### Проблемы с памятью

Если контейнеры падают из-за нехватки памяти:
1. Увеличьте лимит памяти в Docker Desktop (Settings → Resources)
2. Остановите ненужные контейнеры: `docker-compose down`

#### Очистка и пересборка

Если что-то пошло не так, можно полностью пересобрать:

```bash
# Остановить и удалить все
docker-compose down -v

# Удалить образы
docker-compose rm -f

# Пересобрать с нуля
docker-compose build --no-cache
docker-compose up -d
```

### 10. Мониторинг

#### RabbitMQ Management Console

Откройте http://localhost:15672 в браузере:
- Username: `guest`
- Password: `guest`

Здесь можно:
- Просматривать очереди сообщений
- Мониторить производительность
- Управлять exchanges и bindings

#### Проверка здоровья сервисов

```bash
# Health check endpoints
curl http://localhost:3000/health
curl http://localhost:3001/health
curl http://localhost:3004/health
curl http://localhost:3005/health
```

### 11. Остановка проекта

```bash
# Остановить все сервисы (сохраняет данные)
docker-compose stop

# Остановить и удалить контейнеры (сохраняет данные)
docker-compose down

# Остановить и удалить все, включая volumes (удалит данные БД!)
docker-compose down -v
```

## Следующие шаги

После успешного запуска:

1. Проверьте, что все сервисы работают: `docker-compose ps`
2. Откройте Frontend: http://localhost:80
3. Проверьте API Gateway: http://localhost:3000/health
4. Изучите документацию API в `/docs` endpoint

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что порты не заняты другими приложениями





